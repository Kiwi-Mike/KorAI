services:
  ollama:
    image: ollama/ollama  # Replace with the actual image name
    container_name: OllamaKorAI
    ports:
      - "40004:40004"
    volumes:
      - ./data:/data  # Optional: persist models or data
    command: ["ollama", "pull", "gemma:2b"]  # Pull the required model on startup